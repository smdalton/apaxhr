version: "3.7"
services:
  web:
    build: .
    stdin_open: true
    tty: true
    volumes:
      - .:/code
    env_file:
      - ./.env.prod
    ports:
      - "8000:8000"
    command: ["./scripts/start_prod.sh"]