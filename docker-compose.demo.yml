version: "3.7"
services:
  web:
    build: .
    stdin_open: true
    #tty: true
    volumes:
      - .:/code
    env_file:
      - scripts/.env.prod
    ports:
      - "8000:8000"
    command: python3 start_demo_server.py