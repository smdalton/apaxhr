version: "3.7"
services:
  web:
    build: .
    stdin_open: true
    #tty: true
    volumes:
      - .:/code
    env_file:
      - scripts/.env.dev
    ports:
      - "80:8000"
    command: python3 start_demo_server.py